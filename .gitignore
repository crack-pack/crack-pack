# dependencies / build output
node_modules/
dist/
*.log
.DS_Store

# Claude Code local config
.claude/

# Raw research source data — large, not part of the library
# (the distilled sheet data lives in src/sets/alpha/sheets.json)
Alpha/
Alpha 2/
*.numbers
*.xlsx
*.pdf

# web demo build cache (regenerated by build-web.mjs)
web/.img-cache.json
